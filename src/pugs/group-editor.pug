.sidebar-header
    div(data-path="group/prompt") 1 group selected
    .button-row
        button(data-action="group/deselect") deselect
        button(data-action="group/recolor") recolor
        button(data-action="group/select") select cards
        button(data-action="group/delete") ungroup
